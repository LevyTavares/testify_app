// Temporary shims to satisfy TypeScript in this environment.
// In a fully configured Expo project, React Native provides its own types.
// If your editor still complains, ensure node_modules are installed and TypeScript picks them up.
declare module "react-native";
declare module "expo-sqlite";
declare module "expo-sharing";
